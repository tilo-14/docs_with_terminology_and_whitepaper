---
title: "JSON RPC Methods"
description: "Complete JSON RPC API reference for ZK Compression"
---

## Overview

Helius Labs maintains the canonical RPC API and indexer implementation. The following pages outline all ZK Compression methods extending Solana's default JSON RPC API.

<Info>
  All methods follow the standard JSON RPC 2.0 protocol. Base URL varies by
  network: - **Devnet**: `https://api.devnet.solana.com` - **Mainnet**:
  `https://api.mainnet-beta.solana.com`
</Info>

<Note>
  📋 **Quick Navigation**: Click any method below to expand its documentation.
  All methods return JSON RPC 2.0 compliant responses.
</Note>

---

## <Icon icon="users" size={20} /> Account Methods

<AccordionGroup>

<Accordion title="getCompressedAccount">
Get details of a compressed account by its hash.

**Parameters:**

- `accountHash` (string): The hash of the compressed account

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedAccount",
  "params": ["accountHashExample"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getCompressedAccountsByOwner">
Get all compressed accounts owned by a specific address.

**Parameters:**

- `owner` (string): The owner's public key

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedAccountsByOwner",
  "params": ["ownerPublicKey"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getMultipleCompressedAccounts">
Get multiple compressed accounts in a single request.

**Parameters:**

- `accountHashes` (array): Array of account hashes

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getMultipleCompressedAccounts",
  "params": [["hash1", "hash2", "hash3"]]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

</AccordionGroup>

---

## <Icon icon="coins" size={20} /> Balance Methods

<AccordionGroup>

<Accordion title="getCompressedBalance">
Get the compressed SOL balance for a specific owner.

**Parameters:**

- `owner` (string): The owner's public key

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedBalance",
  "params": ["ownerPublicKey"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getCompressedBalanceByOwner">
Get compressed balances grouped by owner.

**Parameters:**

- `owner` (string): The owner's public key

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedBalanceByOwner",
  "params": ["ownerPublicKey"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

</AccordionGroup>

---

## <Icon icon="credit-card" size={20} /> Token Account Methods

<AccordionGroup>

<Accordion title="getCompressedTokenAccountBalance">
Get the compressed token balance for a specific token account.

**Parameters:**

- `account` (string): The token account address

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedTokenAccountBalance",
  "params": ["tokenAccountAddress"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getCompressedMintTokenHolders">
Get all token holders for a specific compressed mint.

**Parameters:**

- `mint` (string): The mint address

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedMintTokenHolders",
  "params": ["mintAddress"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getCompressedTokenBalancesByOwnerV2">
Get compressed token balances by owner (version 2).

**Parameters:**

- `owner` (string): The owner's public key

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedTokenBalancesByOwnerV2",
  "params": ["ownerPublicKey"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getCompressedTokenAccountsByOwner">
Get compressed token accounts by owner.

**Parameters:**

- `owner` (string): The owner's public key

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedTokenAccountsByOwner",
  "params": ["ownerPublicKey"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getCompressedTokenAccountsByDelegate">
Get compressed token accounts by delegate.

**Parameters:**

- `delegate` (string): The delegate's public key

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedTokenAccountsByDelegate",
  "params": ["delegatePublicKey"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

</AccordionGroup>

---

## <Icon icon="repeat" size={20} /> Transaction Methods

<AccordionGroup>

<Accordion title="getTransactionWithCompressionInfo">
Get transaction details with compression-specific information.

**Parameters:**

- `signature` (string): The transaction signature

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getTransactionWithCompressionInfo",
  "params": ["transactionSignature"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

</AccordionGroup>

---

## <Icon icon="certificate" size={20} /> Proof Methods

<AccordionGroup>

<Accordion title="getCompressedAccountProof">
Get a Merkle proof for a compressed account.

**Parameters:**

- `accountHash` (string): The account hash

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressedAccountProof",
  "params": ["accountHash"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getMultipleCompressedAccountProofs">
Get Merkle proofs for multiple compressed accounts.

**Parameters:**

- `accountHashes` (array): Array of account hashes

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getMultipleCompressedAccountProofs",
  "params": [["hash1", "hash2", "hash3"]]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getMultipleNewAddressProofs">
Get proofs for multiple new addresses.

**Parameters:**

- `addresses` (array): Array of new addresses

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getMultipleNewAddressProofs",
  "params": [["address1", "address2", "address3"]]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getValidityProof">
Get a validity proof for compressed state.

**Parameters:**

- `stateHash` (string): The state hash

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getValidityProof",
  "params": ["stateHash"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

</AccordionGroup>

---

## <Icon icon="pen-nib" size={20} /> Signature Methods

<AccordionGroup>

<Accordion title="getCompressionSignaturesForAccount">
Get compression signatures for a specific account.

**Parameters:**

- `account` (string): The account address

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressionSignaturesForAccount",
  "params": ["accountAddress"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getCompressionSignaturesForAddress">
Get compression signatures for a specific address.

**Parameters:**

- `address` (string): The address

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressionSignaturesForAddress",
  "params": ["address"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getCompressionSignaturesForOwner">
Get compression signatures for a specific owner.

**Parameters:**

- `owner` (string): The owner's public key

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressionSignaturesForOwner",
  "params": ["ownerPublicKey"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getCompressionSignaturesForTokenOwner">
Get compression signatures for a token owner.

**Parameters:**

- `tokenOwner` (string): The token owner's public key

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getCompressionSignaturesForTokenOwner",
  "params": ["tokenOwnerPublicKey"]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getLatestCompressionSignatures">
Get the latest compression signatures.

**Parameters:**

- `limit` (number, optional): Maximum number of signatures to return

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getLatestCompressionSignatures",
  "params": [100]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getLatestNonVotingSignatures">
Get the latest non-voting signatures.

**Parameters:**

- `limit` (number, optional): Maximum number of signatures to return

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getLatestNonVotingSignatures",
  "params": [100]
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

</AccordionGroup>

---

## <Icon icon="server" size={20} /> Indexer Methods

<AccordionGroup>

<Accordion title="getIndexerSlot">
Get the current indexer slot.

**Parameters:**
None

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getIndexerSlot",
  "params": []
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

<Accordion title="getIndexerHealth">
Get the health status of the indexer.

**Parameters:**
None

**Example Request:**

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "getIndexerHealth",
  "params": []
}
```

Placeholder content - detailed implementation will be added here.

</Accordion>

</AccordionGroup>

---

## ⚠️ Error Codes

<Warning>
  Handle these common error scenarios in your application for better user
  experience.
</Warning>

| Code   | Message              | Description                                  |
| ------ | -------------------- | -------------------------------------------- |
| -32600 | Invalid Request      | The JSON sent is not a valid Request object  |
| -32601 | Method not found     | The method does not exist / is not available |
| -32602 | Invalid params       | Invalid method parameter(s)                  |
| -32603 | Internal error       | Internal JSON-RPC error                      |
| -32000 | Account not found    | The compressed account was not found         |
| -32001 | Invalid account hash | The provided account hash is invalid         |

---

## 💡 Best Practices

<CardGroup cols={2}>
  <Card title="Commitment Levels" icon="shield-check">
    Use appropriate commitment levels: `processed` (fastest), `confirmed`
    (balanced), `finalized` (most reliable)
  </Card>
  <Card title="Rate Limiting" icon="gauge-high">
    Implement retry logic and respect rate limits. Public endpoints: 100 req/s,
    Private: 1000+ req/s
  </Card>
  <Card title="Batch Requests" icon="layer-group">
    Use batch requests when possible to improve efficiency and reduce API calls
  </Card>
  <Card title="Caching" icon="database">
    Cache frequently accessed data to reduce API calls and improve performance
  </Card>
</CardGroup>

---

## 🚀 Next Steps

<CardGroup cols={2}>
  <Card title="SDK Methods" icon="code" href="/api-reference/sdk-methods">
    Explore the TypeScript SDK methods for easier integration
  </Card>
  <Card title="FAQ" icon="circle-question" href="/help/faq">
    Find answers to common questions and troubleshooting tips
  </Card>
</CardGroup>
