---
title: "getIndexerHealth"
description: "Returns an error if the indexer is stale by more than a configurable number of blocks. Otherwise, it returns ok"
api: "POST https://mainnet.helius-rpc.com?api-key=<api_key>/"
---

## getIndexerHealth

Returns an error if the indexer is stale by more than a configurable number of blocks. Otherwise, it returns ok

### Body

<ParamField body="id" type="string" required>
  An ID to identify the request. **Possible values:** `test-account`
</ParamField>

<ParamField body="jsonrpc" type="string" required>
  The version of the JSON-RPC protocol. **Possible values:** `2.0`
</ParamField>

<ParamField body="method" type="string" required>
  The name of the method to invoke. **Possible values:** `getIndexerHealth`
</ParamField>

### Response

<ResponseField name="200" type="string">
  ok if healthy **Default:** `ok` **Possible values:** `ok`
</ResponseField>

<ResponseField name="429" type="object">
  Exceeded rate limit.
</ResponseField>

<ResponseField name="500" type="object">
  The server encountered an unexpected condition that prevented it from
  fulfilling the request.
</ResponseField>

<RequestExample>

```http HTTP
POST / HTTP/1.1
Host: mainnet.helius-rpc.com
Content-Type: application/json
Accept: */*
Content-Length: 65

{
  "id": "test-account",
  "jsonrpc": "2.0",
  "method": "getIndexerHealth"
}
```

</RequestExample>

<ResponseExample>

```json 200 Success
ok
```

</ResponseExample>
